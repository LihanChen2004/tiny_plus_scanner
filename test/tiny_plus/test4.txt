{ This program contains errors }
int x, y;
bool flag;
string message;

read x;
if x > 0 then
    y := x * 2;
    flag := true;
    message := 'Positive number; { Missing closing quote for string }
else
    y := -x;
    flag := false;
    message := 'Negative number';
end
write y;
write message;